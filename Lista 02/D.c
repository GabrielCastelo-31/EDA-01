#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include <stdio.h>
#define MAX 1001;

int main()
{
    int length = MAX;
    char *s = malloc(length * sizeof(char));

    while (1)
    {
        scanf("%s", s);
        if (s[0] == '0')
            break;

        
    }
    return 0;
}
